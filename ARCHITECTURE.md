# Word Journal 架构与改造说明

## 当前框架

- **后端**：Python 3 + Flask，负责词库、账户、设置、AI 生成和知乎内容 API。
- **前端**：原生 HTML/CSS/ES Modules，无构建步骤，适合本地运行和快速迭代。
- **数据**：JSON 词库 + 本地 settings/auth 文件；生成结果存于浏览器本地状态。
- **生成链路**：词卡 + 知乎选题 → prompt_templates → OpenAI 兼容模型 → 结构化英语日报 → 练习/日报存档。

## 目标架构

知乎能力通过 `backend/zhihu_adapter.py` 统一封装。默认实现仍使用现有 HTTP 服务，
因此无 Secret 时可以继续浏览公开故事/知识；需要接入官方 Skill 或新的问答模型时只需替换适配器，
前端请求和提示词格式保持不变。

文章生成把单词卡作为硬约束，把知乎标题/摘要/正文作为灵感素材，要求模型改写而不是复制原文，
并在结果中保留来源标记，便于追溯。

## UI 方向

界面采用知乎蓝色的“学习工作台”布局：浅灰蓝背景、白色卡片、知乎蓝行动色，生成区突出选题、词汇池和生成动作；阅读页保留轻量报刊层级，但内容定位为日常科普与轻娱乐日报。

## OAuth 扩展

后续个性化方案见 `OAUTH_PLAN.md`。当前提交版不模拟知乎登录，也不收集用户知乎凭证。
